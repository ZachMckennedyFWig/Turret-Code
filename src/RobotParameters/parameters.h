#ifndef parameters_H
#define parameters_H

extern const int buffer_size; 
extern const double camera_y_res, camera_x_res, camera_horizontal_fov, camera_vertical_fov, target_height, camera_angle, camera_height, fps, 
                 latency_frames, wheel_diameter, wheel_ratio, ticks_per_rev, chassis_width, odom_update_speed, turret_max, turret_min, PI;
extern bool aim_at_goal;

#endif
